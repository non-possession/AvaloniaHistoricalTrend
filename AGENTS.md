# PMTestApplication Agent Instructions

本项目面向工业上位机组件开发学习，以及后续自动化、电气工程师接手维护。

## 代码风格硬规则

1. 优先使用显式、直白的写法。
2. 只有当类型一眼就能看懂时，才使用 `var`。
3. 不为了简洁而牺牲可维护性。
4. 复杂业务逻辑避免过度使用 LINQ、模式匹配、复杂 `switch expression`。
5. 状态机、报警逻辑、参数联动必须做到一眼能看懂。
6. 关键业务逻辑必须加注释，尤其是设备状态切换、定时刷新、报警判断。
7. 项目默认服务于后续工程维护，代码风格应降低理解门槛，而不是追求“高级感”。

## 语法糖使用限制

以下写法默认不用或少用，除非确实能提高可读性：

1. 过度使用 `var`
2. 到处使用表达式体成员
3. 过度花哨的模式匹配
4. 过长的链式 LINQ
5. 复杂的 `switch expression`

## C# 上位机开发约定

1. 界面状态、设备状态、报警状态尽量拆成清晰的字段和方法。
2. 定时器刷新逻辑要拆清楚“刷新时间”“采集数据”“报警判断”这几步。
3. 按钮命令、设备启停、报警开关等逻辑优先使用显式方法名。
4. 当逻辑会影响现场调试时，代码必须优先可读、可排查、可修改。

## MVVM 架构约定

1. 继续以 MVVM 架构扩展代码。
2. Model 只管数据逻辑。
3. View 只管显示数据和转发必要的界面事件。
4. ViewModel 控制显示什么，并根据 Model 的变化更新显示状态。
5. 图表控件、文件系统等强外部依赖可以通过服务或适配器隔离，不直接塞进 ViewModel。

## 当前项目分层职责

1. `Model/State`
   - 保存趋势变量、时间窗口、量程、刷子、十字光标等纯数据状态。
   - 保存历史/实时模式、实时采集窗口、采集频率、Y1/Y2 轴分组、采样点等纯状态。
   - 不引用 Avalonia 控件，不读写文件，不调用 ScottPlot。
2. `Coordinator/Domain Service`
   - 处理业务规则，例如变量显隐、轴选择、量程换算、刷子范围约束、时间窗口约束。
   - 处理历史/实时模式切换、实时窗口滚动、采样数据装载、Y1/Y2 候选变量集合规则。
   - 轴分组规则必须从状态/配置读取，不能在业务逻辑里重新硬编码“前 4 个 Y1、后 4 个 Y2”。
   - 不直接操作 XAML 控件，不负责文件写入和图表控件渲染。
3. `ViewModel`
   - 承接页面展示状态和页面级动作入口。
   - 根据 Model/Coordinator 的变化生成界面需要显示的内容。
   - 历史/实时切换、Now、Y1/Y2 变量选择、时间窗口输入等页面动作优先进入 ViewModel。
   - 不直接依赖 ScottPlot 控件，不直接操作 Avalonia 控件。
4. `View`
   - 只显示数据、接收用户操作、转发控件事件。
   - 可以保留必须依赖控件尺寸、鼠标位置、Avalonia 定时器的桥接逻辑。
   - 定时器 Tick 只能转发“采集一次”等动作，不能直接生成数据或写数据库。
   - 不写业务规则，不直接承担 CSV 导出、配置保存、数据库写入、随机采集等应用动作。
5. `Adapter/Presenter/Service`
   - `ScottPlotTrendAdapter` 隔离 ScottPlot 渲染和坐标换算。
   - `TrendCsvExportService` 隔离 CSV 文件写入。
   - `TrendPlotImageExportService` 隔离当前画布图片保存。
   - `TrendPlotPrintService` 隔离打印入口；当前实现为保存图片后交给系统默认图片程序打开。
   - `TrendOutputFileNameService` 统一生成 CSV、图片等输出文件名，避免各服务重复拼接。
   - `TrendWorkbenchSettingsService` 隔离配置文件读写。
   - `ITrendDataStore` 定义历史数据读写接口，当前 `SqliteTrendDataStore` 是 SQLite 实现，后续 MySQL 或其他存储必须新增实现而不是改 View。
   - `ITrendDataCollector` 定义采集接口，当前 `RandomTrendDataCollector` 是默认模拟采集器，真实设备采集必须新增实现。
   - 应用服务负责协调数据存储、数据库切换、采集频率更新、数据采集、历史/实时模式、导出/保存/打印等页面动作。
   - 应用服务负责把 ViewModel 的页面动作转成 Coordinator/Service 调用。
   - 文件导出、图片保存、打印等动作完成后，ViewModel 负责给出成功或失败提示，并显示绝对输出路径或目标目录。

后续新增功能时，优先放入对应层。除非功能必须依赖 Avalonia 控件实例或像素坐标，否则不要直接塞进 `MainWindow.axaml.cs`。

## 每次进入项目时的执行要求

1. 先阅读本文件。
2. 新增或修改代码时，优先遵守本文件而不是追求更短的写法。
3. 如果发现现有代码不符合这些规则，优先做可读性重构，再继续功能开发。
